# =============================================================================
# RF Sentinel v15 â€” Requirements
# =============================================================================
# Production: core + ML + DSP + SDR + Web
# Testing: add pytest or run with unittest

# Core dependencies (absolute must-haves)
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Web UI & Networking
Flask>=3.0.0
gevent>=23.9.0          # optional: async WSGI server (fast Flask)
requests>=2.31.0

# DSP & Visualization
matplotlib>=3.7.0

# Machine Learning backends (Gate 3 classifier)
# Note: first available in this order wins â€” LightGBM > XGBoost > RandomForest (built-in)
lightgbm>=4.0.0         # optional but recommended (3-5x faster inference)
xgboost>=2.0.0          # optional (fallback if lightgbm unavailable)

# Signal processing & anomaly detection
hmmlearn>=0.3.0         # Hidden Markov Models (FHSS/spoofing detection)

# SDR & Hardware Interfaces
hackrf>=0.1.0           # HackRF One driver
# Alternative if hackrf fails on your platform:
# pyhackrf>=1.0.0



# Testing (optional â€” run tests without these using unittest)
pytest>=7.4.0           # Modern test runner: pytest test_sdr_sentinel.py
